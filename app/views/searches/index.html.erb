
<div id="sidebar1">
<!-- this probably should be rendered as a partial see show -->
	<h2>MLS Searches</h2>
	<p id="select">Show: <%= link_to 'All', searches_path %> <% if admin? %> | 
	<%= link_to 'New search', new_search_path %><% end %> </p>
	<% for search in @searches %>
		<h4><%= link_to h(search.title), search %></h4>
	<% end %>
</div>
<div id="mainContent">
	<h2>All Searches</h2>
	<%= render :partial => @articles %>
</div>